services:
  - type: web
    name: gangshan-battle
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GAME_DATA_DIR
        value: /tmp/gangshan-battle-data
